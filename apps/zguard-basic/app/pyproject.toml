[project]
name = "zguard-basic"
version = "0.1.0"
description = "Istio ext-authz enforcing per-host HTTP Basic authentication"
readme = "README.md"
authors = [
    { name = "fastlorenzo", email = "git@bernardi.be" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "bcrypt>=4.0.0",
]

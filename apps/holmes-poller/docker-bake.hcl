target "docker-metadata-action" {}

variable "APP" {
  default = "holmes-poller"
}

variable "VERSION" {
  default = "0.1.1"
}

variable "SOURCE" {
  default = "https://github.com/fastlorenzo/containers"
}

group "default" {
  targets = ["image-local"]
}

target "image" {
  inherits = ["docker-metadata-action"]
  args = {
    VERSION = "${VERSION}"
  }
  labels = {
    "org.opencontainers.image.source" = "${SOURCE}"
  }
}

target "image-local" {
  inherits = ["image"]
  output = ["type=docker"]
  tags = ["${APP}:${VERSION}"]
}

target "image-all" {
  inherits = ["image"]
  platforms = [
    "linux/amd64"
  ]
}

---
# yaml-language-server: $schema=https://raw.githubusercontent.com/goss-org/goss/master/docs/schema.yaml
schemaVersion: "2.0.0"
commandTests:
  - name: poller module importable
    command: python
    args: ["-c", "import poller"]
    envVars: ["PYTHONPATH=/app"]
    exitCode: 0
  - name: dependencies importable
    command: python
    args: ["-c", "import requests, redis"]
    exitCode: 0
